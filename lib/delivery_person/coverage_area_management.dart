import 'package:flutter/material.dart';

class coverage_area_management extends StatelessWidget {
  const coverage_area_management({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Coverage Area Management'),
        backgroundColor: Colors.white,
        elevation: 1,
        foregroundColor: Colors.black,
      ),
      body: const Center(child: Text('Coverage Area Management')),
    );
  }
}
