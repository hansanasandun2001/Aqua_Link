import 'package:flutter/material.dart';
class create_ads extends StatelessWidget {
  const create_ads({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
