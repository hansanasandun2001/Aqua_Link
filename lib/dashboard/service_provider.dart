import 'package:flutter/material.dart';
class service_provider extends StatelessWidget {
  const service_provider({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
